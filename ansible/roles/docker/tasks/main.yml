- name: install `pip`
  ansible.builtin.apt:
    name: python3-pip
    state: present

- name: install docker from `install_docker.yml`
  import_tasks: install_docker.yml

- name: install docker-compose from 'install_compose.yml'
  import_tasks: install_compose.yml
